/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['imgur.com', 'images2.imgbox.com', 'imgbox.com'],
  },
};

module.exports = nextConfig;
